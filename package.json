{
  "name": "pdf-semantic-search-rag-netlify",
  "version": "1.0.0",
  "description": "Complete PDF Semantic Search & RAG Service - Ready for Netlify Deployment",
  "main": "index.html",
  "scripts": {
    "start": "echo 'This is a static site - no build required!' && echo 'Upload all files to Netlify for deployment.'",
    "build": "echo 'No build required - this is a static site!' && echo 'All files are ready for Netlify deployment.'",
    "deploy": "echo 'Deploy to Netlify by uploading all files to netlify.com'",
    "test": "echo 'Static site - no tests required'"
  },
  "keywords": [
    "pdf",
    "semantic-search",
    "rag",
    "ai",
    "netlify",
    "static-site",
    "document-search",
    "question-answering"
  ],
  "author": "Your Name",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "your-repo-url"
  },
  "homepage": "https://your-site-name.netlify.app",
  "engines": {
    "node": ">=14.0.0"
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not dead"
  ],
  "devDependencies": {},
  "dependencies": {},
  "netlify": {
    "build": {
      "publish": ".",
      "command": "echo 'Static site - no build required'"
    },
    "headers": {
      "/*": {
        "X-Frame-Options": "DENY",
        "X-XSS-Protection": "1; mode=block",
        "X-Content-Type-Options": "nosniff"
      }
    }
  }
}
